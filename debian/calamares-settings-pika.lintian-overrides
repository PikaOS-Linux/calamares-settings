# The .desktop file uses bash to run firefox as the local
# user. bash is included in the system.
calamares-settings-vanilla: desktop-command-not-in-package usr/share/applications/vanilla-calamares.desktop bash
